#include <iostream>
#include <vector>

using namespace std;

int main()
{
    // cout.setf(ios::fixed,ios::floatfield);
    //cout.precision(0);
    int u, d;
    while (cin >> u >> d)
    {
        cout << "" << u + d << endl;
    }
}